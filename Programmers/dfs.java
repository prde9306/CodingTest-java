package Programmers;
//여행 경로
public class dfs {
    String[] answer = {};
    boolean[]used;
    public String[] solution(String[][] tickets) {
        used= new boolean[tickets.length]

        dfs(String[][] tickets, String[]answer, int count)
        for(int i=0; i=<tickets.length; i++){
            if(tickets[i][i+1]==tickets1[i+1][i]){
                aswer.append(tickets[i+1][i])
                used[i]=true;
                else{
                    tickets
                }
            }

        }
        dfs()

        return answer;
    }
}
